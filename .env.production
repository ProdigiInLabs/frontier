# Production build defaults (public values only — see .env.example).
VITE_SITE_URL=https://prodiginl.com
VITE_DEMO_MODE=true
VITE_CONTACT_EMAIL=info@prodiginl.com
# Preserves the GA4 property that the previous site loaded unconditionally;
# it now loads only after consent.
VITE_ANALYTICS_PROVIDER=ga4
VITE_GA_MEASUREMENT_ID=G-ZNTS89WZ2Y
